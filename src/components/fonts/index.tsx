import localFont from 'next/font/local';

const OverusedGrotesk = localFont({
  src: '../../fonts/OverusedGrotesk-VF.woff2',
  variable: '--font-overused-grotesk',
  display: 'swap',
});

export default OverusedGrotesk;
